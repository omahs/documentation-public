---
title: "useDaily"
---

Get daily user volume

// TODO Tech docs

### Example

```ts
const { data } = useDaily({
  startDate: new Date(Date.now() - 86400000 * 40), // 40 days ago
  endDate: new Date() // today
});
```
