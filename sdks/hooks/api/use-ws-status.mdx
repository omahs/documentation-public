---
title: "useWsStatus"
---

Receive WebSocket client status.

## Example

```ts
const status = useWsStatus();

useEffect(() => {
  if (status === WsNetworkStatus.Connected) {
    // do something
  }
}, [status]);
```
