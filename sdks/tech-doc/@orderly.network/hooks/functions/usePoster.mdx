[**@orderly.network/hooks v1.5.10**](/sdks/tech-doc/@orderly.network/hooks/README) • **Docs**

***

[Orderly SDKs](/sdks/tech-doc/packages) / [@orderly.network/hooks](/sdks/tech-doc/@orderly.network/hooks/README) / usePoster

# Function: usePoster()

> **usePoster**(`data`, `options`?): `object`

Generates a poster image based on position information. You can set the size, background color, font color, font size, and content position of the poster.

## Parameters

• **data**: [`DrawOptions`](/sdks/tech-doc/@orderly.network/hooks/type-aliases/DrawOptions)

The options to draw the poster

• **options?**

• **options.ratio?**: `number`

The ratio of the poster

## Returns

`object`

### canCopy

> **canCopy**: `boolean`

Browser if supports copy image to clipboard

### copy()

> **copy**: () => `Promise`\<`void`\>

#### Returns

`Promise`\<`void`\>

### download()

> **download**: (`filename`, `type`, `encoderOptions`?) => `void`

Downloads the poster as an image

#### Parameters

• **filename**: `string`

• **type**: `string` = `"image/png"`

• **encoderOptions?**: `number`

#### Returns

`void`

### error

> **error**: `null` \| `Error`

### ref()

> **ref**: (`ref`) => `void`

The ref to the canvas element, you should pass this ref to the canvas element

#### Parameters

• **ref**: `null` \| `HTMLCanvasElement`

#### Returns

`void`

### toBlob()

> **toBlob**: (`type`?, `encoderOptions`?) => `Promise`\<`null` \| `Blob`\>

Converts the poster to a blob

#### Parameters

• **type?**: `string`

• **encoderOptions?**: `number`

#### Returns

`Promise`\<`null` \| `Blob`\>

### toDataURL()

> **toDataURL**: (`type`?, `encoderOptions`?) => `string`

Converts the poster to a data URL

#### Parameters

• **type?**: `string`

• **encoderOptions?**: `number`

#### Returns

`string`

## Example

```tsx
const { ref, toDataURL, toBlob, download, copy } = usePoster({
  backgroundColor: "#0b8c70",
  backgroundImg: "/images/poster_bg.png",
  color: "rgba(255, 255, 255, 0.98)",
  profitColor: "rgb(0,181,159)",
  // ...
});
```

## Defined in

[packages/hooks/src/usePoster.ts:21](https://github.com/OrderlyNetwork/js-sdk/blob/554ae704783e3b83c40bb234a899e8f02987d9b6/packages/hooks/src/usePoster.ts#L21)
